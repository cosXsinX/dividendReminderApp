<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MyDividendReminder" parent="android:Theme.Material.Light.NoActionBar" />
    
    <!-- Custom activity transition styles to exclude AppBar -->
    <style name="Theme.MyDividendReminder.NoAppBarTransition" parent="Theme.MyDividendReminder">
        <item name="android:windowEnterTransition">@android:transition/fade</item>
        <item name="android:windowExitTransition">@android:transition/fade</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
    </style>
    
    <!-- Alternative transition style with slide animation but excluding AppBar -->
    <style name="Theme.MyDividendReminder.SlideTransition" parent="Theme.MyDividendReminder">
        <item name="android:windowEnterTransition">@android:transition/slide_top</item>
        <item name="android:windowExitTransition">@android:transition/slide_bottom</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
    </style>
</resources>